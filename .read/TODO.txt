调查：如何进一步预备 分布式/微服务 的调整，目前已改用雪花 id

各类展示信息做一层缓存

写个微服务，定期同步数据库与七牛云中图片结果，清除未记载在数据库的图片

检查各模块有没有性能问题

setting 把 jwt 过期时间改回来

思考：把 tags、content_parts、content_head 改为用 river 传输

Id 发放改为号段模式：先去数据库申请一段 ID，比如 100000 ~ 100999，然后这 1000 个 ID 在内存里自己发，发完再去数据库申请下一段
检查redis有没有持久化，避免数据回退

可选用更新的交互式验证码：http://gocaptcha.wencodes.com/guide/ 
验证码是ddos攻击的良好接口之一，因为无需登录即可从服务器获取高成本响应；
目前的防ddos策略：
- 登录失败 2 次后才出现验证码（同时发起 cloudflare JS 质询）
- 每个 ip 每秒只能生成 1 次验证码
- 验证 10 次失败则拉黑 1 分钟。

并发安全：先查再建这种非原子操作，在并发环境下，有概率导致重复建数据。

后续优化登录校验性能：
1. 把“用户状态 + token_version + role + deleted_at”做短 TTL 缓存
2. 不要每次都查 user_models
3. 把 session 有效性做短 TTL 缓存，不要每次都查 user_session_models
4. 只把“撤销、改密、封禁”这些动作当成缓存失效触发器。如果访问量再上来，再考虑把黑名单和 session 状态合并成更紧凑的 Redis 校验链路。

完善 QQ 登录
QQ登录 - https://video.fengfengzhidao.com/play/17/3391
发送手机验证码需要企业资质，而邮箱验证码可以用个人邮箱。因此，目前项目的设置为必须绑定邮箱后才能依靠邮箱进行密码修改。


需模块化的内容：es_river、image_ref_river、cron_service、


AI 写作助手
可以放在发文和改文流程里，做标题优化、摘要生成、标签推荐、分类建议、SEO 描述生成、错别字修正、语气润色、长文提纲整理。这个最容易见效，也最符合博客后台场景。

站内知识问答
把已发布文章当知识库，做“基于文章内容回答问题”。比如用户问“这个站里有哪几篇讲 Go 中间件？”、“帮我总结作者写过的 ES 相关文章”。这个比普通 AI 对话更有辨识度，也更能提升搜索体验。

语义搜索 / 混合搜索
你现在明显已经有 ES 关键词搜索了，但还偏传统检索。可以升级成“关键词 + 向量召回 + rerank”，支持自然语言搜文，比如“有没有讲高并发缓存击穿处理的文章”，而不只是搜精确词。

评论与内容审核
文章发布审核、评论垃圾内容识别、辱骂/广告检测、低质量评论拦截、给管理员生成审核理由。这类功能和你现有评论、文章审核流天然匹配，而且能直接降低运营成本。

AI 博客助手
你现有聊天系统可以复用，但建议做成“站内 AI 助手”而不是普通陪聊。比如它能回答站内内容问题、推荐文章、解释某篇文章重点、帮用户生成评论草稿、帮作者回复评论。

个性化推荐
结合浏览历史、收藏、点赞、标签偏好，做“你可能感兴趣的文章”“每周精选摘要”“新文章订阅推荐”。这个适合后面做，因为它需要行为数据和推荐策略一起迭代。

多语言能力
比如自动生成英文摘要、中英双语文章、标题翻译、术语解释。对技术博客挺实用，尤其如果你想做搜索流量扩展。


日志模块整体进行分析，如何重构更好？
日志系统如何更好的存储数据到数据库里，又怎么返回给前端
修改 middleware，jwt 每天登录接入时，如果是第一次被使用，则记录该用户今天登录了。
目前了解 ELK、Clickhouse。


站点配置修改的接口是否已经完善？

了解 ELK ：https://www.cnblogs.com/crazymakercircle/p/18561362
采集层：Beats 全量保留（Filebeat/Metricbeat 等）
↓
缓冲层：Kafka/RabbitMQ 保留（削峰填谷，解耦上下游）
↓
清洗转换层：Logstash 可选/轻量化替代（Vector/Gohangout）
↓
存储检索层：Elasticsearch → ClickHouse 集群（核心替换）
↓
语法转换层：CKibana/Quesma 代理（实现 Kibana 与 ClickHouse 的无缝对接）
↓
可视化交互层：Kibana 全量保留（Discover、Dashboard、告警等能力完全复用）


最新架构使用：
应用日志/容器日志
       ↓
【采集器】Fluent Bit 或 Vector（轻量，比 Beats 简单）
       ↓
【缓冲队列】Kafka（可选，高并发才需要）
       ↓
【清洗/路由】Vector（可选，简单场景可跳过）
       ↓
【存储核心】ClickHouse 集群（真正存日志、做检索、做统计）
       ↓
【可视化平台】Grafana / SigNoz / ClickVisual（类 Kibana 体验）
       ↓
【告警】Grafana Alert 或平台自带告警 https://cloud.tencent.com/developer/article/1807679


最终最简架构：
应用日志
    ↓
Fluent Bit（每台机器一个）
    ↓
ClickHouse（单机，以后做集群）
    ↓
Grafana（搜索+图表+告警）


那结合你的方案，制定计划，完整修改当前日志系统：

1. 日志文件分类存放:
    runtime_logs
    用于运行日志、请求日志、错误日志、警告日志、后台任务日志
    login_event_logs
    用于登录成功、失败、登出、刷新 token、风控事件
    action_audit_logs
    用于管理员操作、敏感业务操作、配置变更、删除/封禁等审计事件

2. 不继续“一张大宽表存所有日志”，在 ClickHouse 里也分为三类

3. 获取日志的逻辑也完整分析优化

4. 不要默认把所有请求体和响应体都打进去，只有必要的接口要记录，分析哪些接口需要记录body并写记录代码逻辑，最终汇报告诉我。

5. 编写 docker compose ，加入 Fluent Bit 和 ClickHouse

6. 修改 settings.yaml 等一切跟日志系统有关的内容

7. 做一层日志查询服务，列表与单独查询

8. 不兼容旧的逻辑

9. 应用侧做完整适配修改




不是“某个接口记录了完整的 request/response header 和 body”，而是：

当前没有任何接口会记录完整的请求头、响应头。
代码里我没看到把 header 写进审计日志的地方。

现在实际记录了什么
1. 普通请求日志
middleware/log_middleware.go

这里只记了这些元信息：
request_id
method
path
status_code
latency_ms
ip
user_id
不记 request body，不记 response body，也不记 header。

2. 操作审计日志
service/log_service/action_audit.go
service/log_service/gin_helpers.go

这里支持写：

request_body
response_body
但这是接口代码手动传什么就记什么，不是自动抓原始 HTTP 全量内容。

也就是说：
不是完整 raw body
不是自动记录 header
不是所有接口都有 response body
哪些接口记了 request_body
这些接口调用了 EmitActionAuditFromGin(... RequestBody: ...)：

轮播图新增/删除/更新
api/banner_api/enter.go
文章新增/审核/删除/更新
api/article_api/article_create.go
api/article_api/article_examine.go
api/article_api/article_remove.go
api/article_api/article_update.go
全局通知新增/删除
api/global_msg_api/global_notif_create.go
api/global_msg_api/global_notif_remove.go
图片删除
api/image_api/image_remove.go
站点运行时配置更新
api/site_api/site_info_update.go
分类新增/更新/删除
api/article_api/category/category_create_update.go
api/article_api/category/category_delete.go
标签新增/更新/删除
api/article_api/tags/tag_create_update.go
api/article_api/tags/tag_delete.go
管理员更新用户资料
api/user_api/profile_api/admin_user_info_update.go
但这些 request_body 多数是：

绑定后的 struct
手工组的 map
有些字段还做了裁剪/脱敏
比如文章更新只记 content_length，不会把全文正文直接打进去；AI 配置把 secret_key 记成 ***。
哪些接口记了 response_body
只有一部分删除类接口顺手记了结果摘要，不是完整响应体：

api/banner_api/enter.go
api/article_api/article_remove.go
api/global_msg_api/global_notif_remove.go
api/image_api/image_remove.go
api/article_api/tags/tag_delete.go
api/article_api/category/category_delete.go
这些一般只是类似：

deleted_count
id_list
操作结果摘要
不是完整 response body，也不含 response headers。


目前优化方案：
1. 新增一个专用采集中间件，在Router上挂载的接口就会采集请求体。
2. 日志层再有两个字段，例如： UseRawRequestBody bool \UseRawResponseBody bool
3. 先想“采集并存起来”，第一阶段先只落盘/入库，不大改clickhouse。
4. 中间件中会自动处理信息脱敏。
5. 不能无限制把 body 写进去，大于16kb则取各字段的值截断，保留完整字段名。
6. 一个中间件 + 独立开关，参考：mw.CaptureLogBody(选择其中一个)，可选：{Req, Resp, Both}

