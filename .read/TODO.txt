调查：如何进一步预备 分布式/微服务 的调整，目前已改用雪花 id

各类展示信息做一层缓存

写个微服务，定期同步数据库与七牛云中图片结果，清除未记载在数据库的图片

检查各模块有没有性能问题

setting 把 jwt 过期时间改回来

思考：把 tags、content_parts、content_head 改为用 river 传输

Id 发放改为号段模式：先去数据库申请一段 ID，比如 100000 ~ 100999，然后这 1000 个 ID 在内存里自己发，发完再去数据库申请下一段
检查redis有没有持久化，避免数据回退

可选用更新的交互式验证码：http://gocaptcha.wencodes.com/guide/ 
验证码是ddos攻击的良好接口之一，因为无需登录即可从服务器获取高成本响应；
目前的防ddos策略：
- 登录失败 2 次后才出现验证码（同时发起 cloudflare JS 质询）
- 每个 ip 每秒只能生成 1 次验证码
- 验证 10 次失败则拉黑 1 分钟。

并发安全：先查再建这种非原子操作，在并发环境下，有概率导致重复建数据。

后续优化登录校验性能：
1. 把“用户状态 + token_version + role + deleted_at”做短 TTL 缓存
2. 不要每次都查 user_models
3. 把 session 有效性做短 TTL 缓存，不要每次都查 user_session_models
4. 只把“撤销、改密、封禁”这些动作当成缓存失效触发器。如果访问量再上来，再考虑把黑名单和 session 状态合并成更紧凑的 Redis 校验链路。

完善 QQ 登录
QQ登录 - https://video.fengfengzhidao.com/play/17/3391
发送手机验证码需要企业资质，而邮箱验证码可以用个人邮箱。因此，目前项目的设置为必须绑定邮箱后才能依靠邮箱进行密码修改。


需模块化的内容：es_river、image_ref_river、cron_service、


List优化一次HasMore的形式（各种List列表看看怎么搞）
查询过多的接口，不再走统一 ListQuery，走专用sql模式。
比如说 CommentList ：
  评论列表现在的主要性能问题，不是单条评论逐条查数据库，而是为了组装作者信息、回复目标昵称、是否点赞、好友关系这些字段，登录用户一次请求会拆成很多次 SQL，回复列表还因为两级 preload 更重，所以它的整体开销偏大。
  如果要“能省则省”，最好的办法是把这两个接口改成专用查询，不再走通用 ListQuery + Preload + 批量补查。主查询里直接用 join 把评论作者昵称头像、父评论作者昵称、当前用户是否点赞、当前用户和评论作者关系一次查出来，这样就能去掉 preload、去掉额外的点赞查询和关系查询。
  同时再把当前用户识别改成轻量方式，只取 viewerUserID，不要每次都做完整的用户和 session 校验。最后配合补上更贴近查询条件的复合索引，评论列表就能收敛成少量固定 SQL 加少量 Redis HMGET，这会是当前项目里最值得做的一轮优化。


考虑升级成 PostgreSQL，首先得了解什么是PgSql


User表可以细化分表
UserConfModel、UserStatModel 不变
UserAuthModel：
- Password 
- Email 
- OpenID 
- TokenVersion 
- LastPasswordChangedAt
UserModel：（还可以考虑怎么进一步分化）
- Username 
- Status
- Role
- RegisterSource
UserProfileModel：
- IP 
- Addr
- Nickname 
- Avatar 
- Abstract
- gender
- 签名


接口限制：每日数量限制，禁止重复操作过快


站内知识问答
把已发布文章当知识库，做“基于文章内容回答问题”。比如用户问“这个站里有哪几篇讲 Go 中间件？”、“帮我总结作者写过的 ES 相关文章”。这个比普通 AI 对话更有辨识度，也更能提升搜索体验。

语义搜索 / 混合搜索
你现在明显已经有 ES 关键词搜索了，但还偏传统检索。可以升级成“关键词 + 向量召回 + rerank”，支持自然语言搜文，比如“有没有讲高并发缓存击穿处理的文章”，而不只是搜精确词。

评论与内容审核
文章发布审核、评论垃圾内容识别、辱骂/广告检测、低质量评论拦截、给管理员生成审核理由。这类功能和你现有评论、文章审核流天然匹配，而且能直接降低运营成本。

AI 博客助手
你现有聊天系统可以复用，但建议做成“站内 AI 助手”而不是普通陪聊。比如它能回答站内内容问题、推荐文章、解释某篇文章重点、帮用户生成评论草稿、帮作者回复评论。

个性化推荐
结合浏览历史、收藏、点赞、标签偏好，做“你可能感兴趣的文章”“每周精选摘要”“新文章订阅推荐”。这个适合后面做，因为它需要行为数据和推荐策略一起迭代。

多语言能力
比如自动生成英文摘要、中英双语文章、标题翻译、术语解释。对技术博客挺实用，尤其如果你想做搜索流量扩展。


了解 ELK ：https://www.cnblogs.com/crazymakercircle/p/18561362
最新架构使用：
应用日志/容器日志
       ↓
【采集器】Fluent Bit 或 Vector（轻量，比 Beats 简单）
       ↓
【缓冲队列】Kafka（可选，高并发才需要）
       ↓
【清洗/路由】Vector（可选，简单场景可跳过）
       ↓
【存储核心】ClickHouse 集群（真正存日志、做检索、做统计）
       ↓
【可视化平台】Grafana / ClickVisual（类 Kibana 体验）
       ↓
【告警】Grafana Alert 或平台自带告警 https://cloud.tencent.com/developer/article/1807679


docker compose 里存在密码，怎么办?
后续全面转向环境变量配置， https://www.doubao.com/chat/38419183522472450


站点配置修改的接口是否已经完善？

新增 UserStatModel ： 
- ViewCount 
- FansCount 
- FollowCount

FansCount / FollowCount
做同步事务更新冗余字段。

ViewCount 大概方向：
- 统计逻辑：用户主页每1个独立访客访问+1，同一人当日多次访问仅计1次，游客访问不统计
- 去重方案：Redis HSETNX 记录{访客ID:被访者ID}(具体key细节设置你来补充)，整组key每日0点过期
- 数据库：新增UserViewDailyModel（Model、UserID、ViewerUserID、ViewDate），建立唯一索引
- DB 更新方式：SQL事务完成两个操作(原子插入日访问记录+更新总ViewCount)，无需提前查询
- 执行流程：Redis写入式判重 → 无重复则数据库INSERT+UPDATE → DB 失败时回滚 Redis
“HSETNX 抢占式判重 + DB 事务两条 SQL + DB 唯一索引兜底 + 事务失败后最佳努力 HDEL 回滚 Redis”

接口升级：api\user_api\profile_api\user_base_info.go 返回字段增加
ViewCount
FansCount
FollowCount
FavoritesVisibility
FollowVisibility
FansVisibility
HomeStyleID


校准系统
新增 cron 任务：定时校准冗余数据。
方案：平时校准不需要每天重算整个历史，只需获取今日有新插入的数据，然后做增量更新即可。


推荐机制怎么做呢？以下是基础设想
文章推荐：当天浏览量最多的


水平分表：https://www.wolai.com/fengfeng/pXg4Q6X9giMznrZiQg63jG


缓存优化中间件：把一些接口的响应记录到redis里，10秒之内走缓存
https://video.fengfengzhidao.com/play/17/3599
考虑：
 - 要加主动不走缓存的选项
 - 缓存如何主动过期
 - 如何防止恶意请求，导致大量同一缓存内容出现不同key进入缓存。
复用：
 - 复用audit_body_middleware，改名成（capture_middleware），挂载时只能选择是否拦截 request的body和Header（选项变成 None，Head，Body，Both）。
 - 到了接口内真正需获取 capReqBody/Header bool 来判断日志是否记录请求体/头
 - 检测 gin.Context 里有没有 capRespBody/Header bool 来选择是否拦截响应体/头
 - 修改时减少历史遗留的不必要的代码
 - 没必要解耦的代码不强行解耦
以下接口可以加入（需判断还有哪些可以加入）：
 - BannerList
 - 标签列表


- 注意：
  - 固定 prompt 严格锁定输出边界，要求输出json格式并正则解析，正则解析工具放入ai_service/ai_utils.go，所有ai_service通用 
  - 不要对很短的选区强行输出一堆建议，长度太短时直接提示“内容过短，建议选中完整句或完整段落”；

- 鼠标选中内容后的即时操作（走 SSE 流式输出）：

  - 具体功能：

    - 内容改写：鼠标划线选中内容，弹出选项：内容润色、语法改正、风格转换；
      - 支持`内容润色`、`语法改正`、`风格转换`三种模式；
      - 返回“替换后的文本”；
      - 润色功能最怕“把意思改跑了”。所以 prompt 必须明确要求：不新增原文没有的事实，不改变人名、时间、数字、结论，不乱补专业知识；
      - 润色只允许在表达、语法、结构、语气上优化。风格转换也一样，必须要求“保留原始信息和观点，只调整语气和句式”。
    - 问题诊断：专门分析选中文本哪里有问题，指出内容不足之处：如专业性过强不易阅读、内容缺少、逻辑紊乱等。
      - 返回「问题类型 + 严重度 + 说明 + 原文证据 + 可执行建议」的结构化结果
      - 例如“问题类型=可读性，严重度=中，说明=句子过长且术语密集，证据=第2句连续包含3个专业术语，建议=拆成两句并补一个通俗解释”。

  - 传入时，还要传入一点上下文，上下文不用太长，前后各 300 到 800 字足够，不然成本会上去。

    ```bash
    {
    	mode,
    	selection_text,  # 上限 3500 字, 下限 25 字, 必选
    	refix_text,      # 上限 800 字, 可选
    	suffix_text,     # 上限 800 字, 可选
    	article_title,   # 上限 50 字, 必选
    	target_style,    # 风格转换时启用, 可选
    }
    # 上下限放 const
    ```

- 全文质量评分：文章总体质量打分，指出不足之处，指导创作者进行修改。（走同步完整内容输出）

  - 拆成两个能力

    - 一个是“总体质量评分”，返回总分、维度分、主要问题、优先修改项；
    - 另一个是“结构化写作建议”，更偏创作指导，比如“标题不够聚焦”“开头没有建立问题”“论证顺序混乱”“结尾缺少收束”。
    - 可以合并成一个接口返回，但内部 prompt 最好按统一 JSON 结构产出，不然前端很难稳定展示。
    - 至少分成这 6 个维度：
      - 清晰度：是否容易理解
      - 结构性：章节安排和逻辑推进是否顺
      - 完整度：论点、解释、例子是否够
      - 可读性：句子长度、术语密度、节奏
      - 说服力：论证是否扎实
      - 语言规范：语法、病句、表达准确性
    - 长文的总分最好不是简单平均，而是带权重，比如：
      - 结构性、清晰度权重高一些
      - 语言规范权重稍低一些
      - 如果是教程/科普，完整度权重再提高
    - 分数区间建议保守，严格管控分数区间：常规文章 60-80 分，优质文章 81-88 分，精品文章 89-95 分，杜绝 96 分以上虚高评分；
    - 要避免“评分忽高忽低”，所以评分 prompt 要写死标准，最好给模型明确 rubric。
    - 避免重复问题被放大，出现重复、相似的问题，则做去重归并，最后只保留最重要的前0~6个问题。

  - 返回结构大概意思：

    ```bash
    {
    	总分,
    	维度分:[{分,理由}],
    	主要问题:[{
    		原文位置[{第几段,引用原文10~25个字左右}],
    		问题说明,
    		修改建议,
    	}], # 前0~6个
    	总评价与建议
    }
    ```

  - 长文处理：

    - 可处理长度文章：直接全文评分，15w字以内。
    - 超长文：
      - 超出15w字的，平均分段文章，使文章每段<=14w字
      - 每次传入都带完整保留标题、各级小标题（开头结尾就不用带了，万一开头结尾就超长呢？）
      - 第一段时，单独进行一次完整[评分+该段概括+完整]，然后把 [评分+该段摘要+问题]+下一段 交给大模型，继续生成
      - 第二段之后，将 [评分+之前概括 + 新该段文章概括] 交给大模型，生成 [评分+该段概括+问题]
      - 最后一段，不再生成概况，输出最终结果
      - 补充1：[评分+该段摘要]在同一次输出里获取，避免两次访问大模型加成本
      - 补充2：prompt可采取拼接的方式，第一次、第二次到倒数第二次、最后一次 分别拼接同一个核心评分prompt
      - 补充3：避免重复问题被放大，出现重复、相似的问题，则做去重归并，最后只保留最重要的前0~6个问题。

可以按现在 AI 子包风格（可进一步按业务范围拆分）
在 ai_writing 包拆成：
ai_writing_types.go：请求响应结构、枚举。
ai_writing_rewrite.go：内容润色/内容改正/风格转换。
ai_writing_diagnose.go：选中片段问题分析。
ai_writing_prompt.go：统一放 prompt 模板。

在 ai_scoring 包拆成：
ai_scoring.go：整篇评分与建议。
ai_scoring_prompt.go：统一放 prompt 模板。



前端交互上怎么做才顺手
选中文本弹出的菜单要非常轻，最多三四个动作，不要做成二级菜单迷宫。点击后最好先弹一个小 loading，再展示“替换内容 / 追加建议 / 保留原文”三种操作。对诊断类结果，不建议自动替换原文，而是旁边列出问题卡片，点击卡片时高亮原句。整篇评分则更像右侧面板，展示“总分 + 维度雷达图/条形图 + 主要问题 + 一键跳转定位到段落”。如果能定位到段落，会比只有文字报告强很多。